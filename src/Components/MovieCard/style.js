import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    height: 100,
    backgroundColor: '#fff',
    marginBottom: 8,
    borderColor: '#ccc',
    borderWidth: 1,
    borderRadius: 6,
  },
  tags: {
    flexDirection: 'row',
  },
});
